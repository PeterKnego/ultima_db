[package]
name = "ultima-db"
version = "0.1.0"
edition = "2024"
description = "Embedded in-memory B-tree store with MVCC"

[dependencies]
thiserror = "2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "store_bench"
harness = false
