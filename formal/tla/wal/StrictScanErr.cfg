\* OWED PROPERTY, expected RED. Not a canary for a green below it: a named
\* record that a known gap is still there. Constants are the DEFAULT
\* Standalone shape -- SingleWriter, Consistent, strict scan (FsWrite here
\* stands for WalWrite::PerEntry; tolerance is CoalescedPrealloc-only,
\* src/store.rs:1017-1022) -- so this is reachable on the configuration a
\* user gets without opting into anything.
\* It also violates under WalCrashMW.cfg's constants; SingleWriter is
\* committed because it is the more pointed statement.
SPECIFICATION Spec
INVARIANT StrictScanErrLosesDurableAck
CHECK_DEADLOCK FALSE
CONSTANTS
  MaxCommits = 2
  Tables = {t1, t2}
  Durability = "Consistent"
  SinkKind = "FsWrite"
  WriterMode = "SingleWriter"
  MUTATION = "NONE"
