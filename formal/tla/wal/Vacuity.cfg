\* Vacuity canary. This run MUST report a violation.
\* If NoCommitEverPromotes ever HOLDS, nothing in the model promotes and
\* every safety verdict from WalCrash.cfg is trivially, silently true.
SPECIFICATION Spec
INVARIANT NoCommitEverPromotes
CHECK_DEADLOCK FALSE
CONSTANTS
  MaxCommits = 2
  Tables = {t1, t2}
  Durability = "Consistent"
  SinkKind = "FsWrite"
  WriterMode = "SingleWriter"
  MUTATION = "NONE"
