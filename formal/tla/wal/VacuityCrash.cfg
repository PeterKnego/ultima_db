\* Crash-reachability canary (SingleWriter). This run MUST report a violation.
\* If NoCrashThenRecover HOLDS, no behaviour ever crashes and then recovers,
\* and every RecoverySound verdict from WalCrash.cfg is checked over zero
\* crash behaviours -- a green with no content.
SPECIFICATION Spec
INVARIANT NoCrashThenRecover
CHECK_DEADLOCK FALSE
CONSTANTS
  MaxCommits = 2
  Tables = {t1, t2}
  Durability = "Consistent"
  SinkKind = "FsWrite"
  WriterMode = "SingleWriter"
  MUTATION = "NONE"
