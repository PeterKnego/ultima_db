\* Vacuity canary for the MultiWriter baseline. This run MUST report a
\* violation. Every green is paired with a canary that goes red first.
SPECIFICATION Spec
INVARIANT NoCommitEverPromotes
CHECK_DEADLOCK FALSE
CONSTANTS
  MaxCommits = 2
  Tables = {t1, t2}
  Durability = "Consistent"
  SinkKind = "FsWrite"
  WriterMode = "MultiWriter"
  MUTATION = "NONE"
