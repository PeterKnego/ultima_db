/target
*.iml
/.idea

/.claude

# Flamegraph artifacts
flamegraph.svg
perf.data
perf.data.old

# Stray local Claude settings sometimes copied to repo root (the real one lives under .claude/)
/settings.local.json

# SDD scratch (briefs, reports, ledger, diffs)
.superpowers/

# Local secrets (e.g. MISTRAL_API_KEY for the Leanstral experiment)
.env
